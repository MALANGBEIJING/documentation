.. attribute:: name
   :noindex:

   如果 `type` 为 `object`，则调用的方法。如果 `type` 为 `action`，则加载的动作的 :term:`XMLID <external identifier>`，可以是原始格式或 `%(XMLID)d` 格式。

   :requirement: 可选
   :type: str
   :default: `''`
