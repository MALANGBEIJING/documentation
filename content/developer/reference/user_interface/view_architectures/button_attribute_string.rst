.. attribute:: string
   :noindex:

   如果没有 `icon`，则为按钮的文本；否则为图标的 `alt` 文本。

   .. example::
      .. code-block:: xml

         <button type="object" name="action_create_new" string="Create document"/>

   :requirement: 可选
   :type: str
   :default: `''`
